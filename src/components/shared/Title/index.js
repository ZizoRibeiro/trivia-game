import styled from 'styled-components/native';

import { colors } from '../../../styles';

export const Title = styled.Text`
  font-size: 25px;
  font-weight: bold;
  color: ${colors.yellow};
  padding-top: 50px;
  text-align: center;
`;
